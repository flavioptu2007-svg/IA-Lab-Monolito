# ═══════════════════════════════════════════════════════════════
# Dependências consolidadas no pyproject.toml
# ═══════════════════════════════════════════════════════════════
# Este arquivo existe apenas para compatibilidade legada.
# Use pyproject.toml para gerenciar dependências.
#
# Instalação:
#   pip install -e .                    # Apenas core
#   pip install -e ".[dev]"             # Core + dev tools
#   pip install -e ".[stt,tts]"         # Core + STT + TTS
#   pip install -e ".[all]"             # Tudo exceto OpenVINO
# ═══════════════════════════════════════════════════════════════

-e .[dev]
